<div class="sm:col-span-2 p-3.5 items-center text-center">
  <div class="card w-96 bg-primary shadow-xl">
    <% @temples.each do |temple| %>
      <figure><%= image_tag 'sample.png', size: '300x200' %></figure>
        <div class="card-body">
         <%
=begin%>
 中心から<%= "#{@greentea.get_distance(@latitude, @longitude)} "%>m 
<%
=end%>
          <h2 class="card-title">
            <div class="card-actions justify-end">
              <%= link_to temple.name, temple_path(temple), class: 'button btn btn-error', style: 'color: white'%>
          </h2>
            </div>
        </div>
    <% end %>
  </div>
</div>