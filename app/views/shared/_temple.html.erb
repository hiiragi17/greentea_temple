<% @temples.each do |temple| %>
  <div class="gird gird-cols-3 sm:col-span-2 p-3.5">
    <div class="card 2l bg-primary shadow-xl items-center text-center">
      <div class='p-3.5'>
        <figure><%= image_tag 'sample.png', size: '300x200' %></figure>
      </div>
      <div class="card-body">
        <%
=begin
        %>
 中心から<%= "#{@greentea.get_distance(@latitude, @longitude)} " %>m
<%
=end
%>
        <h2 class="card-title">
        <div class="card-actions justify-end">
          <div class="p-3.5">
            <%= temple.address %><br>
            <%= temple.holiday %><br>
            <%= link_to temple.name, temple_path(temple), class: 'btn btn-wide btn-error', style: 'color: white' %>
          </div>
        </div>
        </h2>
      </div>
    </div>
  </div>
<% end %>
